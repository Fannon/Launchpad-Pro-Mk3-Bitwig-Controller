const config = {
  /**
   * Minimum velocity to trigger a pad / button
   */
  triggerThreshold: 1,

  defaultButtonColor: 2,
  /**
   * Color for pad / button highlights
   */
  highlightColor: 3,
  /**
   * Time in ms for pad / button highlight when pressed
   */
  triggerHighlightMs: 200,
};
